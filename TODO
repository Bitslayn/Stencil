3rd Rewrite:
	✔ Rewrite layers and add sprite layer
	☐ Decouple layer rendering from elements
	☐ Rewrite elements
	☐ Rewrite widgets

Flex queue:
	✘ Bake element property groups
	✔ Element redraw shapes to queue immediate, siblings, or children whenever properties change
	☐ Validate accuracy and performance of flex queue
	✘ Create separate diff for layer styles

Features:
	✔ Add absolute element positioning
	✔ Fix sounds not working for screens attached to the GUI
	✔ Add overlay to elements that are being redrawn
	☐ Add GN's element generator from table
	☐ Finish all widgets

Finalization:
	☐ Comment and document everything
	☐ Final cleanup pass
